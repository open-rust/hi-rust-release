name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - run: echo hello

  build2:

    runs-on: centos-latest

    steps:
      - run: cat /etc/os-release

permissions:
  contents: write
